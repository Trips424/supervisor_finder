import 'package:flutter/material.dart';

class StaffAvailabilityChip extends StatelessWidget {
  const StaffAvailabilityChip({
    super.key,
    required this.acceptingStudents,
  });

  final bool acceptingStudents;

